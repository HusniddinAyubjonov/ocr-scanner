# OCR Scanner

Next.js + Tesseract.js document OCR (English, Russian, Tajik + digits). Monorepo scaffolded with pnpm workspaces.

## Packages

- `packages/web` — Next.js app (OCR scanner UI)
- `packages/server` — reserved for the future API

## Development

```bash
pnpm install
pnpm run web
```
